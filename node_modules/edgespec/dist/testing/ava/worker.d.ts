import { InitialWorkerData } from "./types.js";
import { SharedWorker } from "ava/plugin";
export declare class Worker {
    private initialData;
    private startBundlerPromise;
    constructor(initialData: InitialWorkerData);
    handleTestWorker(testWorker: SharedWorker.TestWorker): Promise<void>;
    private startBundler;
}
