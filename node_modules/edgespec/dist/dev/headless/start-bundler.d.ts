import { ResolvedEdgeSpecConfig } from "../../config/utils.js";
import { ChannelOptions } from "birpc";
import { HttpServerRpcFunctions, type BundlerRpcFunctions } from "./types.js";
export interface StartHeadlessDevBundlerOptions {
    config: ResolvedEdgeSpecConfig;
    initialRpcChannels?: ChannelOptions[];
}
/**
 * Start a headless EdgeSpec dev bundler. It will continuously watch your code and rebuild on changes.
 * For this to be useful, you'll probably want to also start a headless dev server.
 * This must be run within a native context (Node.js, Bun, or Deno).
 */
export declare const startHeadlessDevBundler: ({ config, initialRpcChannels, }: StartHeadlessDevBundlerOptions) => Promise<{
    stop: () => Promise<void>;
    birpc: import("birpc").BirpcGroup<HttpServerRpcFunctions, BundlerRpcFunctions>;
}>;
