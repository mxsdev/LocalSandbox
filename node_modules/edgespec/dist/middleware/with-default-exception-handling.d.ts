import { Middleware } from "../middleware/types.js";
import { Logger } from "./with-logger.js";
interface CreateWithDefaultExceptionHandlingOptions {
    coloredLogs?: boolean;
    logWhen?: (status: number, error: unknown) => boolean;
    includeStackTraceInResponse?: boolean;
}
/**
 *
 */
export declare const createWithDefaultExceptionHandling: ({ coloredLogs, logWhen, includeStackTraceInResponse, }?: CreateWithDefaultExceptionHandlingOptions) => Middleware<{
    logger?: Logger;
}>;
export {};
