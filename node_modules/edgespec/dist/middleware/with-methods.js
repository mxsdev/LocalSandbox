import { MethodNotAllowedError } from "./http-exceptions.js";
export const withMethods = (methods) => (req, ctx, next) => {
    if (!methods.includes(req.method)) {
        throw new MethodNotAllowedError(methods);
    }
    return next(req, ctx);
};
