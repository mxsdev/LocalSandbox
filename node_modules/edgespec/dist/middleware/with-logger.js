/**
 * Attaches a provided logger to ctx.logger.
 * `ctx.logger` is used by internal EdgeSpec middleware when provided (instead of `console`).
 */
export const createWithLogger = (logger) => (req, ctx, next) => {
    ctx.logger = logger;
    return next(req, ctx);
};
