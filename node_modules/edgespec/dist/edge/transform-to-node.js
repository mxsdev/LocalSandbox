import { buildToNodeHandler, } from "@edge-runtime/node-utils";
import primitives from "@edge-runtime/primitives";
const dependencies = {
    ...primitives,
    Uint8Array,
};
export const transformToNodeBuilder = (options) => buildToNodeHandler(dependencies, options);
