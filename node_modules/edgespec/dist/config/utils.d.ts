import { EdgeSpecConfig } from "../config/config.js";
import { SetRequired } from "type-fest";
export interface ResolvedEdgeSpecConfig extends EdgeSpecConfig {
    rootDirectory: string;
    tsconfigPath: string;
    routesDirectory: string;
}
/**
 * Resolves relative paths and sets defaults for any missing values.
 */
export declare const resolveConfig: (config: SetRequired<EdgeSpecConfig, "rootDirectory">) => ResolvedEdgeSpecConfig;
export declare const loadConfig: (rootDirectory: string, overrides?: Partial<EdgeSpecConfig>) => Promise<ResolvedEdgeSpecConfig>;
