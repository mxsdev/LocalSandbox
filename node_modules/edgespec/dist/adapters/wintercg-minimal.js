export const addFetchListener = (edgeSpec) => {
    addEventListener("fetch", async (event) => {
        // TODO: find a better way to cast this
        const fetchEvent = event;
        fetchEvent.respondWith(await edgeSpec.makeRequest(fetchEvent.request));
    });
};
