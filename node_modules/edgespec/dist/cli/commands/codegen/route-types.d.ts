import { BaseCommand } from "../../../cli/base-command.js";
import { ResolvedEdgeSpecConfig } from "../../../config/utils.js";
export declare class CodeGenRouteTypes extends BaseCommand {
    static paths: string[][];
    outputPath: string;
    run(config: ResolvedEdgeSpecConfig): Promise<void>;
}
